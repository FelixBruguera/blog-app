<%= stylesheet_link_tag 'index' %>
<h1>Articles</h1>

<ul>
    <% @articles.each do |article| %>
        <li>
            <div id='article-title'><%= link_to article.title, article %></div>
            <p ><%= article.body %></p>
        </li>
  <% end %>
</ul>

<p id='new'><%= link_to "New Article", new_article_path %></p>
